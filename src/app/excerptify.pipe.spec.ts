import { ExcerptifyPipe } from './excerptify.pipe';

describe('ExcerptifyPipe', () => {
  it('create an instance', () => {
    const pipe = new ExcerptifyPipe();
    expect(pipe).toBeTruthy();
  });
});
